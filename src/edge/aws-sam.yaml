AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: RAMP edge Lambda@Edge bot-redirect handler (viewer-request)

Parameters:
  ExchangeUrl:
    Type: String
  # Absolute URL of the exchange's unified /.well-known/ramp.json. The edge
  # resolves the Ed25519 delivery-URL verify key from its public_keys[].
  ExchangeManifestUrl:
    Type: String

Resources:
  RampEdgeFn:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: ramp-edge
      Handler: aws-lambda.handler
      Runtime: nodejs20.x
      CodeUri: dist/aws/
      MemorySize: 128
      Timeout: 5
      Role: !GetAtt RampEdgeRole.Arn
      AutoPublishAlias: live
      Environment:
        Variables:
          EXCHANGE_URL: !Ref ExchangeUrl
          EXCHANGE_MANIFEST_URL: !Ref ExchangeManifestUrl

  RampEdgeRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
                - edgelambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
